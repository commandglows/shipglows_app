# Local validation bundle. Production installs the two imported snippets through
# apply-caddy-snippets.sh so unrelated Caddy sites remain untouched.
import caddy/00-shipglows-global.Caddyfile
import caddy/shipglows-personal-cloud.Caddyfile
